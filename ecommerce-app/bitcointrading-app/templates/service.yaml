apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitcointrading-app.fullname" . }}
  labels:
    {{- include "bitcointrading-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitcointrading-app.selectorLabels" . | nindent 4 }}
